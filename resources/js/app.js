import './bootstrap';
import { NavbarAnimation } from "./components/navbar.js"

/*
* This file contains the global script for all pages of the application,
* It works with the import of component files.
* */

window.addEventListener('load', () => {
    NavbarAnimation()
})
